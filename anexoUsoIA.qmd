---
title: "Anexo: Diario de Interacción con IA."
---

:::{.callout-important title="Nota Importante."}
En esta sección no se han incluido todas las interacciones que se han tenido con la IA. Las que no están son porque:

1. Se han perdido (no las encontré).
2. Son muy parecidas a otras interacciones ya explicadas.

Al final de esta parte hay un resumen del porcentaje aproximado de uso de la IA para cada bloque.

Finalmente, es necesario mencionar que no se ha identificado ninguna alucinación de la IA. Es cierto que hay veces que ha fallado en sus salidas, pero no ha sido por su culpa. Todas las veces que falló fue porque me generó código para visualizar un grafo, y no usaba las variables que yo tenía simplemente porque no se las facilité.
:::

## Prompt 1: Configuración libro quarto.

### Motivo.

Se le ha pedido que genere el apartado `format:` del libro quarto con el que se va a desarrollar el proyecto.

### Prompt (Modelo Opus 4.8 Extra).

```text         
créame un muy buen campo apartado de format para un trabajo científico
y profesional. El documento es un libro de quarto, solo necesito el 
campo de format y debe compilar en pdf y html. Tiene que producir 
una salida PROFESIONAL, ELEGANTE y MINIMALISTA
```

### Output.

Aquí tienes un bloque format pensado para un Quarto book con doble salida (PDF + HTML), buscando ese aire sobrio y tipográficamente cuidado:


```yml
html:
    theme: cosmo    # limpio y neutro; alternativas: litera, flatly
    fontsize: 1.05em
    linestretch: 1.6            # interlineado generoso = más legible
    highlight-style: github     # resaltado de código discreto
    code-copy: true
    code-overflow: wrap
    code-block-bg: true
    code-block-border-left: "#d0d0d0"
    toc: true
    toc-depth: 3
    toc-title: "Contenido"
    number-sections: true
    anchor-sections: true
    smooth-scroll: true
    link-external-icon: true
    link-external-newwindow: true
    fig-align: center
    fig-cap-location: bottom
    tbl-cap-location: top
    df-print: paged             # tablas de datos paginadas y limpias
    grid:                       # proporciones equilibradas de página
      sidebar-width: 260px
      body-width: 820px
      margin-width: 260px

  pdf:
    documentclass: scrbook # KOMA-Script: el estándar para libros serios
    classoption: [oneside] # usa [twoside] si lo vas a encuadernar
    papersize: a4
    geometry:
      - top=25mm
      - bottom=25mm
      - inner=30mm
      - outer=25mm
      - heightrounded
    fontsize: 11pt
    linestretch: 1.4
    pdf-engine: xelatex # tipografía moderna y soporte de fuentes
    colorlinks: true
    linkcolor: black            # enlaces internos discretos
    urlcolor: "RoyalBlue"
    citecolor: "RoyalBlue"
    toc: true
    toc-depth: 3
    toc-title: "Contenido"
    number-sections: true
    highlight-style: github
    code-block-bg: "#f7f7f7"
    fig-cap-location: bottom
    tbl-cap-location: top
    keep-tex: false
    # mainfont: "TeX Gyre Pagella" # descoméntalas para look tipo Palatino
    # sansfont: "TeX Gyre Heros"
    # monofont: "JetBrains Mono"
```

Tres cosas que conviene que sepas:

El PDF necesita una distribución LaTeX. Si no la tienes, instala TinyTeX con `quarto install tinytex` (es ligera y suficiente). Con xelatex y las fuentes comentadas, por defecto usa Latin Modern, que ya viene con LaTeX y compila sin instalar nada extra.
Las fuentes están comentadas a propósito. Si las activas, deben estar instaladas en tu sistema o falla la compilación. TeX Gyre Pagella/Heros suelen venir con TeX Live; JetBrains Mono la tendrías que instalar aparte. Esa terna (serif elegante + sans humanista + mono legible) es la que más eleva el resultado, pero pruébalo solo cuando tengas las fuentes.
`oneside` vs `twoside`. Lo dejé en `oneside` porque para lectura en pantalla evita los márgenes asimétricos y las páginas en blanco entre capítulos. Si el destino es imprenta y encuadernado, cambia a `twoside` y el `geometry` con `inner`/`outer` ya te dará márgenes correctos para el lomo.

Si quieres dar un paso más en la parte HTML, puedo prepararte un `theme.scss` con tu paleta y tipografía para afinar colores, espaciados y bloques de código por encima de lo que permite el YAML.

### Valoración.

La parte del `format` me pareció correcta, la probé y me gustó el resultado. Sin embargó la conversación siguió para que me generara ese archivo **theme.scss** que me propuso.

## Prompt 2: Generación archivo theme.scss.

### Motivo.

Se le ha pedido que genere el archivo **theme.scss** del libro quarto con el que se va a usar para generar el html del proyecto. Este archivo lo propuso la IA en la primera interacción.

### Prompt (Modelo Opus 4.8 Extra).
```text      
vale, creame un scss para la parte de html que sea igual de elegante,
minimalista y profesional
```

### Output.

Para activarlo, en tu `_quarto.yml` cambia la línea del tema dentro de `format: html:`:

```yaml    
theme: [cosmo, theme.scss]
```

El orden importa: `cosmo` aporta la base de Bootstrap y tu `theme.scss` la sobrescribe encima. Guarda el archivo en la raíz del proyecto (junto al `_quarto.yml`).

Tres decisiones de diseño que conviene que entiendas:

El sistema tipográfico IBM Plex (sans + serif + mono) es lo que más eleva el resultado. El cuerpo va en sans para leer cómodo en pantalla, pero los títulos van en serif: ese contraste serif/sans es el truco editorial que da el aire de "libro científico" sin recargar nada. Al ser una sola familia coordinada, todo se ve coherente. Se cargan desde Google Fonts, así que la primera compilación necesita conexión.

Un solo acento (azul tinta `#15506b`). En minimalismo, la elegancia viene de la restricción: fondo blanco, texto casi negro y un único color que marca enlaces, TOC activo y citas. Si quieres un tono más académico clásico, cambia `--accent` y `$link-color`/`$primary` a un granate (`#7a1f2b`).
Las tablas usan estilo "booktabs" (solo líneas horizontales gruesas arriba/abajo y bajo la cabecera, sin rejilla vertical). Es la convención de las publicaciones científicas serias y es lo que hace que una tabla se vea profesional en lugar de "de hoja de cálculo".

Si más adelante quieres que el PDF y el HTML compartan exactamente la misma paleta de acento, puedo darte el `include-in-header` de LaTeX equivalente para que los enlaces del PDF usen ese mismo azul tinta.

[Más fichero **theme.scss** usado en el proyecto]

### Valoración.

De nuevo, me pareció correcto el resultado que produjo, así que no se siguió preguntando.

---

## Prompt 3: Consulta sobre qué había que hacer en el Bloque 2.

### Motivo.

Se le ha preguntado qué es lo que pedía la tarea del Bloque 2, y se le ha pedido los pasos para hacerlo (no la solución).

### Prompt (Modelo Opus 4.8 Extra).
```text      
**Tarea:** Construir la red de usuarios en un ç
**grafo de coparticipación**. 
Dos productos están conectados si han sido reseñados por los mismos *N* 
usuarios. Debéis justificar la elección de *N* basándoos en la densidad 
del grafo resultante.

que es esto y cuales son los pasos para hacerlo
```

### Output.

Voy a explicarte primero qué te están pidiendo conceptualmente y luego los pasos concretos en tu stack (R/igraph/tidygraph).

Qué es

Partes de datos de reseñas, que de forma natural son una red bipartita (o de dos modos): tienes dos tipos de nodos —usuarios y productos— y una arista usuario↔producto cuando ese usuario ha reseñado ese producto.

Un grafo de coparticipación es la proyección de esa red bipartita sobre uno solo de los conjuntos de nodos, en tu caso los productos. Colapsas los usuarios y conectas dos productos cuando comparten reseñadores. El número de reseñadores que comparten dos productos es el peso de la arista.

El matiz importante de la tarea es el umbral N: no conectas dos productos por compartir un solo usuario, sino solo si comparten al menos N reseñadores en común. Ese N es un filtro que controla cuántas aristas sobreviven y, por tanto, la densidad del grafo final.

Los pasos

- Preparar los datos. Necesitas una tabla de pares (usuario, producto) —una fila por reseña—. Limpia duplicados (un mismo usuario que reseñó el mismo producto dos veces cuenta como uno solo).
- Construir la matriz de incidencia bipartita B de tamaño productos × usuarios, donde B[p,u] = 1 si el usuario u reseñó el producto p. Lo más eficiente es una matriz dispersa (Matrix::sparseMatrix).
- Proyectar sobre productos. El truco está en una multiplicación matricial: C = B %*% t(B). Entonces C[i,j] es exactamente el número de usuarios que reseñaron ambos productos i y j (la diagonal es cuántos reseñadores tiene cada producto, que descartas).
- Aplicar el umbral N. Conviertes C en adyacencia binaria: hay arista entre i y j si C[i,j] >= N. Aquí "los mismos N usuarios" se interpreta como al menos N reseñadores compartidos (es la convención estándar).
- Justificar N por densidad. Esta es la parte central de la tarea, te la detallo abajo.
- Construir el grafo final y analizarlo con tus métricas de centralidad y topología (grado, intermediación, componentes, etc.) sobre el grafo ya filtrado.

Cómo justificar la elección de N

La idea es que N es una palanca que controla la densidad, y la densidad determina si el grafo es analizable:

- N muy bajo (p. ej. 1): casi todos los productos quedan conectados → densidad altísima, un "ovillo" (hairball) sin estructura visible.
- N muy alto: apenas quedan aristas → grafo fragmentado en muchos componentes aislados, sin estructura que analizar.

Recuerda la fórmula de densidad para un grafo no dirigido:

{FORMULA DE LA DENSIDAD DE UN GRAFO NO DIRIGIDO}

El argumento que debes escribir en la memoria no es "elegí N=3 porque sí", sino un barrido: calcula la densidad (y conviene añadir el nº de componentes y el tamaño del componente gigante) para un rango de valores de N, los representas, y eliges el N en el punto donde el grafo deja de ser un ovillo pero todavía conserva un componente gigante conexo. Normalmente ese punto es un "codo" en la curva densidad–N. Esa curva es tu justificación.

### Valoración.

Gracias a esta respuesta entendí que era un grafo de coparticipación y supe que tenía que hacer. Seguro que usando librería como `tidygraph` o `igraph` también se podría hacer, pero me pareció muy interesante la propuesta de usar matrices dispersas por eficiencia, así que he decidido usarlas.

---

## Prompt 4: Consulta sobre qué había que hacer en el Bloque 3. Sobre todo en la parte de predicciones.

### Motivo.

Se le ha preguntado qué es lo que pedía la tarea del Bloque 3, en especial en lo relacionado con predecir si un producto era de élite o no. También se le ha pedido los pasos para hacerlo (no la solución).

### Prompt (Modelo Opus 4.8 Extra).
```text      
Reto: Si un producto no tiene categoría asignada, ¿podéis predecirla 
basándoos en la categoría de sus “vecinos” en la red de usuarios? 
(Homofilia). 
Utilizad la estructura del grafo para predecir si un usuario pertenece a 
una categoría “Elite” o para recomendar un enlace (Link Prediction), 
por ejemplo, identificando qué productos suelen adquirirse de forma 
conjunta (co-compra) basándose en la estructura de la red.

que puedo hacer para predecir si un producto es elite o no, y que metrica 
puedo predecir con la Homofilia si no tengo ni categoría ni precio.
```

### Output.

Buena pregunta, porque tiene una trampa conceptual que conviene aclarar antes de tocar código.

#### El malentendido a resolver

La homofilia no es algo que "predices": es una propiedad que **mides sobre un atributo que ya tienes**. Es el principio ("los conectados se parecen") que *justifica* propagar etiquetas. Por eso, si no tienes ni categoría ni precio, no es que te falte la homofilia — es que te falta el atributo sobre el cual medirla. La solución es **derivar la etiqueta de la propia estructura del grafo**, que es justo lo que tienes de sobra.

#### Parte 1: definir y predecir "elite" sin atributos

Como no tienes etiqueta natural, defines "elite" de forma **endógena** (topológica). Un producto elite = un nodo en el núcleo denso de la red de co-compra. Opciones, de más simple a más robusta:

- **Grado** (`degree()`): se co-compra con muchos otros. Rápido pero ingenuo.
- **PageRank** (`page_rank()`): importante porque sus vecinos son importantes. Buena opción por defecto.
- **k-core / coreness** (`coreness()`): el núcleo más profundo del grafo. Conceptualmente es la mejor definición de "elite" porque captura densamente interconectados, no solo grado alto.
- **Eigenvector / betweenness**: alternativas según si te interesa influencia o rol de puente.

Eliges una métrica, pones un umbral (p.ej. top 5–10%, o el k-core máximo) y obtienes tu etiqueta binaria `elite ∈ {0,1}`. Esto es tu *ground truth* derivado.

**Para "predecir" (y validar que la homofilia funciona):** ocultas la etiqueta de un subconjunto de nodos y la reconstruyes de dos maneras:

1. **Label propagation / voto de vecinos:** cada nodo oculto recibe la etiqueta mayoritaria de sus vecinos. Si la homofilia es real, esto acierta mucho. En igraph tienes `cluster_label_prop()`, o lo implementas a mano con la lista de adyacencia.
2. **Clasificador sobre features estructurales:** una regresión logística con `degree`, `page_rank`, `coreness`, `transitivity(type="local")` como predictores de `elite`. Mides AUC/accuracy sobre los nodos ocultos.

El punto fino para tu memoria: si defines elite *con* PageRank y luego *predices* elite *con* PageRank como feature, es circular. Para que tenga valor, define elite con una métrica (p.ej. coreness) y predice con **estructura local de vecindario** (label propagation), demostrando que la categoría se contagia por la red.

#### Parte 2: la métrica de homofilia que NO necesita categoría ni precio

Esta es la respuesta directa a tu segunda pregunta:

- **`assortativity_degree(g)`** — mide si los nodos de alto grado tienden a conectarse entre sí. Se calcula **puramente de la topología, sin ningún atributo de nodo**. Es literalmente "homofilia por grado" y es tu mejor opción dado que no tienes atributos. Devuelve un valor en [−1, 1]: positivo = asortativo (homofilia), negativo = disasortativo.
- **`assortativity_nominal(g, types = elite + 1L)`** — una vez derivada la etiqueta elite, mide si elites se conectan con elites. (Ojo: igraph espera índices que empiecen en 1, de ahí el `+1L`.)
- **Edge homophily ratio** (a mano): fracción de aristas cuyos dos extremos comparten clase. Más interpretable para el informe que el coeficiente de Newman.

Resumen: la asortatividad de grado es la métrica de homofilia que puedes reportar **sin etiquetas**, y la asortatividad nominal sobre tu `elite` derivado es la que cierra el círculo conceptual del reto.

#### Bonus: link prediction de co-compra sin atributos

Para la otra mitad del reto (qué productos se compran juntos), los scores clásicos tampoco necesitan atributos, solo topología: **vecinos comunes**, **Jaccard**, **Adamic-Adar** y **preferential attachment**. Calculas el score para pares no conectados, ordenas, y los top son tus enlaces predichos. `igraph::similarity(g, method="jaccard")` te da Jaccard directo; Adamic-Adar suele ir a mano sobre la matriz de adyacencia.

¿Quieres que aterrice alguna de estas en código sobre tu grafo de ~72k productos con N=2? El cuello de botella ahí será la asortatividad y el label propagation a esa escala, que conviene plantear con operaciones sobre matrices dispersas en vez de bucles.

### Valoración.

Gracias a esta respuesta supe qué tenía que hacer. Me pareció muy interesante el predecir si un producto era élite o no usando un modelo de regresión logística, ya que combinaba conceptos vistos en *Fundamentos de Inteligencia Artificial* con los contenidos vistos en esta asignatura. 

Por tanto, decidí seguir las recomendaciones de Claude, ya que como tampoco hemos estudiado mucho de este tema, me pareció interesante ahondar un poco más en esto.

En la segunda parte (qué predecir usando la homofilia) no me pareció muy interesante, así que descarté lo que dijo. Finalmente creé una métrica calidad que era *"alta"* si la valoración promedio del producto era $\ge 4$, y *"baja"* si no lo era.

En el tema del Bonus también se le hizo caso pues, de nuevo, en clase no hemos dado nada de recomendación basada en enlaces que sea escalable a algo de este tamaño.

---

## Prompt 5: Visualización de grafos.

### Motivo.

A la hora de visualizar grafos, se le ha pedido el código a la IA.

### Prompt (Modelo Opus 4.8 Extra).
```text      
{Codigo del grafo}

Usa ggplot2 para visualizar el grafo.

Quiero que:

{características del grafo: 
p.ej: color por comunidades,
tamaño del nodo por grado, etc.}
```

### Output.

{Código para visualizar el grafo}

### Valoración.

Este tipo de interacciones eran bastante directas. El código se copiaba y pegaba y funcionaba normalmente a la primera, aunque algunas veces había que modificarlo un poco para que funcionara, mayoritariamente para cambiar el nombre de las variables que usaba (por los nombres que yo había definido).

---

## Porcentaje de uso de IA en cada fichero.

1. Bloque I: **0 - 5%** (solamente se le pidió que valorara el resultado y que buscara errores y inexactitudes).
1. Bloque II: **20 - 30%** (consultas más relacionadas con temas conceptuales -- *qué* había que hacer, que de implementación).
1. Bloque III: **50 - 60%** (hay un grado elevado de IA ya que no se hizo nada parecido en clase)
1. Comparación PCA vs SNA: **30%** (mayoritariamente ayuda con *cómo* comparar los dos primeros bloques, y un poco de ayuda en la implementación -- sobre todo funciones a utilizar).
